<?xml version="1.0" encoding="UTF-8"?>
<Context path="/apps">
	<Resource 
		name="im_ws"
		auth="Container"
		type="javax.sql.DataSource"
		maxActive="8"
		maxIdle="50"
		maxWait="10000"
		username="${jdbc.username}"
		password="${jdbc.password}"
		driverClassName="${jdbc.driverClassName}"
		validationQuery="SELECT 1"
		removeAbandoned="true"
		removeAbandonedTimeout="60"
		testOnBorrow="true"
		url="${jdbc.url}"/>
		<!-- select * from Utilisateur LIMIT 1 -->
 	
 	
 	<!-- TODO Configuration Postfix Mail server -->
 	<!-- <Resource
		name="im_mail_resources"
		auth="Container"
		type="javax.mail.Session"
		mail.smtp.host="${mail.smtp.host}"
        mail.transport.protocol="${mail.transport.protocol}"
        mail.smtp.port="${mail.smtp.port}"
		mail.smtp.ssl.enable="${mail.smtp.ssl.enable}"
		mail.debug="${mail.debug}" /> -->
		
		
	<!-- <Resource name="mail/Session"
              auth="Container"
              type="javax.mail.Session"
              username="yourusername"
              password="yourpassword"
              mail.debug="true"
              mail.user="yourusername"
              mail.password="yourpassword"
              mail.transport.protocol="smtp"
              mail.smtp.host="your.smtphost.com"
              mail.smtp.auth="true"
              mail.smtp.port="25"
              mail.smtp.starttls.enable="true"/> -->
              
              
     <!-- <Resource 
		name="im_ws"
		auth="Container"
		type="javax.sql.DataSource"
		maxActive="200"
		maxIdle="50"
		maxWait="10000"
		username="appsdb"
		password="appsdb"
		driverClassName="org.postgresql.Driver"
		validationQuery="SELECT 1"
		removeAbandoned="true"
		removeAbandonedTimeout="60"
		testOnBorrow="true"
		url="jdbc:postgresql://localhost:5432/appsdb"/> -->
	
</Context>